before_install:
  - sdk install java 25-open
  - sdk use java 25-open
install:
  - ./gradlew publishToMavenLocal -Pjitpack=true -x test -x check -x checkAddonCompatibility -x checkLegacyCompatibility
