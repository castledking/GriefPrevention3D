before_install:
  - sdk install java 25-open
  - sdk use java 25-open
install:
  - ./gradlew :gp3d-core:jar :jar -Pjitpack=true -x test -x check -x checkAddonCompatibility -x checkLegacyCompatibility
